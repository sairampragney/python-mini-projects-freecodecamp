# Debugging a Division Function

A simple Python debugging exercise that defines a function for dividing two numbers and checks the function by calling it with different inputs.

---

## 📌 About

This project is part of my **Python learning journey** and focuses on understanding how to identify and fix simple programming errors.

The program defines a `divide()` function that takes two numbers, performs division, and returns the result.

The function is then tested with multiple inputs.

---

## 🐛 Debugging Problem

The original code contains formatting and syntax issues that need to be corrected before the program can run properly.

The intended function is:

```python
def divide(a, b):
    result = a / b
    return result
